---
title: "REST SDK Constructor"
description: "QPub.Rest options and instance surface."
---

<ExampleScope defaultGroup="REST" groups={["REST"]} languages={["javascript", "typescript", "go", "curl"]}>

<CodeExamples group="REST">

```javascript
import { QPub } from "@qpub/sdk";

const rest = new QPub.Rest({
  apiKey: process.env.QPUB_API_KEY,
});
```

```typescript
import { QPub } from "@qpub/sdk";

const rest = new QPub.Rest({
  apiKey: process.env.QPUB_API_KEY,
});
```

```go
import (
	"os"

	"github.com/qpubio/qpub-go"
)

rest := qpub.NewRest(qpub.WithAPIKey(os.Getenv("QPUB_API_KEY")))
defer rest.Reset()
```

```curl
# REST calls use Authorization: Basic or Bearer on each request
API_KEY="publicId:secret"
curl -X GET "https://rest.qpub.io/v1/..." \
  -H "Authorization: Basic $(echo -n "$API_KEY" | base64)"
```

</CodeExamples>

## Options

Shares the same options type as Socket for auth/hosts/logging. REST-relevant defaults:

| Option | Default | Description |
|--------|---------|-------------|
| `apiKey` | — | API key |
| `authUrl` / `tokenRequest` | — | Alternate auth |
| `alias` | — | Sent as `X-Alias` when set |
| `httpHost` | `rest.qpub.io` | REST host |
| `httpPort` | `null` | Optional port |
| `isSecure` | `true` | `https` vs `http` |
| `debug` / `logLevel` / `logger` | — | Logging |

Socket-only options (`autoConnect`, reconnect, …) are ignored for REST behavior.

Base URL: `{https|http}://{httpHost}[:port]/v1`

## Instance

| Property / method | Description |
|-------------------|-------------|
| `rest.channels` | Publish API — [Channels](/api/rest-sdk/channels.mdx) |
| `rest.queues` | Queue API — [Queues](/api/rest-sdk/queues.mdx) |
| `rest.auth` | Same token helpers as Socket — `issueToken`, `generateToken`, `createTokenRequest`, `requestToken`, `authenticate` |
| `rest.reset()` | Reset instance |
| `rest.getInstanceId()` | Instance id |

REST does **not** subscribe to channels.

</ExampleScope>
