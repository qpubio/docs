---
title: "Channel"
description: "Named pub/sub topics in QPub."
---

A **channel** is a named string topic. Publishers send messages to a channel; subscribers receive messages from channels they subscribe to.

There is no public “create channel” API. Using a channel name in publish or subscribe is enough.

## Examples of names

```
notifications
chat.lobby
orders.42.status
```

Use clear, hierarchical names. Keep them stable — clients must agree on the same string.

## Access

Access is controlled by [permissions](/core/auth/permissions.mdx) on the API key or JWT, not by channel “types”.

## Clients

<ExampleScope defaultGroup="Socket" groups={["Socket", "REST"]} languages={["javascript", "typescript", "go"]}>

<When group="Socket">

<CodeExamples group="Socket">

```javascript
const channel = socket.channels.get("notifications");
```

```typescript
const channel = socket.channels.get("notifications");
```

```go
ch := socket.Channels.Get("notifications")
```

</CodeExamples>

</When>

<When group="REST">

<CodeExamples group="REST">

```javascript
const channel = rest.channels.get("notifications");
```

```typescript
const channel = rest.channels.get("notifications");
```

```go
ch := rest.Channels.Get("notifications")
```

</CodeExamples>

</When>

</ExampleScope>

See [Using Channels](/core/channel/using.mdx) and [Pub/Sub](/core/pubsub/index.mdx).
